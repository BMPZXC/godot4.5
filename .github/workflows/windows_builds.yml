# Minimal Windows workflow: only build export templates (template_release) with the provided
# disabled_build_options / disabled_classes. Framework and actions are kept close to your original.
# Run via workflow_dispatch or workflow_call.
name: 🏁 Windows Templates (Minimal)
on:
  workflow_call:
  workflow_dispatch:

env:
  # Put the long list of flags here. Keep as a single folded scalar so it's passed as one scons string.
  SCONS_FLAGS: >-
    production=yes
    debug_symbols=no

    # Explicit core disables (also implied by disabled_classes)
    disable_3d=yes
    disable_navigation_3d=yes
    disable_physics_3d=yes
    disable_xr=yes
    module_godot_physics_3d_enabled=no
    module_jolt_physics_enabled=no
    module_msdfgen_enabled=no
    module_openxr_enabled=no

    # Keep ANGLE / AccessKit paths if you still fetch prebuilt binaries
    "angle_libs=${{ github.workspace }}/"
    "accesskit_sdk_path=${{ github.workspace }}/accesskit-c-0.17.0/"

    # Large disabled_classes list (comma separated)
    disabled_classes="AESContext,AStar2D,AStar3D,AStarGrid2D,AnimatableBody2D,AnimatedTexture,AnimationNode,AnimationNodeStateMachinePlayback,AnimationNodeStateMachineTransition,AnimationTree,AspectRatioContainer,AudioEffect,AudioEffectInstance,AudioListener2D,AudioSample,AudioSamplePlayback,AudioStreamGenerator,AudioStreamInteractive,AudioStreamMicrophone,AudioStreamOggVorbis,AudioStreamPlayback,AudioStreamPlayer2D,AudioStreamPlaylist,AudioStreamPolyphonic,AudioStreamRandomizer,AudioStreamSynchronized,BackBufferCopy,BaseMaterial3D,BitMap,Bone2D,BoneMap,ButtonGroup,CPUParticles2D,CallbackTweener,CameraAttributes,CameraFeed,CameraServer,CameraTexture,CanvasGroup,CanvasItemMaterial,CanvasModulate,CanvasTexture,CapsuleShape2D,CharFXTransform,CheckBox,CheckButton,CircleShape2D,ClassDB,CollisionPolygon2D,ColorPalette,ColorPicker,ColorPickerButton,Compositor,CompositorEffect,ConcavePolygonShape2D,ConvexPolygonShape2D,Crypto,CryptoKey,Curve2D,Curve3D,CurveTexture,CurveXYZTexture,DPITexture,DTLSServer,DirAccess,DisplayServer,ENetConnection,EncodedObjectAsID,EngineDebugger,EngineProfiler,Environment,Expression,ExternalTexture,FileAccess,FlowContainer,FogMaterial,FoldableContainer,FoldableGroup,FontVariation,FramebufferCacheRD,GDExtension,GDExtensionManager,GDScriptNativeClass,GLTFAccessor,GLTFAnimation,GLTFBufferView,GLTFCamera,GLTFDocument,GLTFDocumentExtension,GLTFLight,GLTFMesh,GLTFNode,GLTFObjectModelProperty,GLTFPhysicsBody,GLTFPhysicsShape,GLTFSkeleton,GLTFSkin,GLTFSpecGloss,GLTFState,GLTFTexture,GLTFTextureSampler,GPUParticles2D,Geometry2D,Geometry3D,Gradient,GradientTexture1D,GradientTexture2D,GraphEdit,GraphElement,GridContainer,HMACContext,HTTPClient,HTTPRequest,HashingContext,IP,Image,ImageFormatLoader,ImageTexture,ImporterMesh,InputMap,InstancePlaceholder,IntervalTweener,ItemList,JNISingleton,JSON,JSONRPC,JavaClass,JavaClassWrapper,JavaObject,JavaScriptBridge,JavaScriptObject,Joint2D,KinematicCollision2D,KinematicCollision3D,LabelSettings,Light2D,LightOccluder2D,LightmapGIData,Lightmapper,Line2D,LinkButton,Logger,Marshalls,MenuBar,MenuButton,Mesh,MeshConvexDecompositionSettings,MeshDataTool,MeshInstance2D,MeshLibrary,MeshTexture,MethodTweener,MissingNode,MissingResource,MovieWriter,MultiMesh,MultiMeshInstance2D,MultiplayerAPI,MultiplayerSpawner,MultiplayerSynchronizer,Mutex,NativeMenu,NavigationAgent2D,NavigationAgent3D,NavigationLink2D,NavigationMesh,NavigationMeshGenerator,NavigationMeshSourceGeometryData2D,NavigationMeshSourceGeometryData3D,NavigationObstacle2D,NavigationPathQueryParameters2D,NavigationPathQueryParameters3D,NavigationPathQueryResult2D,NavigationPathQueryResult3D,NavigationPolygon,NavigationRegion2D,NavigationServer2D,NavigationServer3D,NinePatchRect,Node3D,Node3DGizmo,Noise,NoiseTexture2D,OS,Occluder3D,OccluderPolygon2D,OggPacketSequence,OggPacketSequencePlayback,OpenXRAPIExtension,OpenXRAction,OpenXRActionMap,OpenXRActionSet,OpenXRBindingModifier,OpenXRExtensionWrapper,OpenXRFutureResult,OpenXRHapticBase,OpenXRIPBinding,OpenXRInteractionProfile,OpenXRInteractionProfileMetadata,OptionButton,PCKPacker,PackedDataContainer,PackedDataContainerRef,PacketPeer,PanoramaSkyMaterial,Parallax2D,ParallaxBackground,ParallaxLayer,ParticleProcessMaterial,Path2D,PathFollow2D,Performance,PhysicalSkyMaterial,PhysicsDirectBodyState2D,PhysicsDirectBodyState3D,PhysicsDirectSpaceState2D,PhysicsDirectSpaceState3D,PhysicsMaterial,PhysicsPointQueryParameters2D,PhysicsPointQueryParameters3D,PhysicsRayQueryParameters2D,PhysicsRayQueryParameters3D,PhysicsServer2D,PhysicsServer2DManager,PhysicsServer3D,PhysicsServer3DManager,PhysicsServer3DRenderingServerHandler,PhysicsShapeQueryParameters2D,PhysicsShapeQueryParameters3D,PhysicsTestMotionParameters2D,PhysicsTestMotionParameters3D,PhysicsTestMotionResult2D,PhysicsTestMotionResult3D,PlaceholderMaterial,PlaceholderTexture2D,PolygonPathFinder,PortableCompressedTexture2D,ProceduralSkyMaterial,ProjectSettings,RDAttachmentFormat,RDFramebufferPass,RDPipelineColorBlendState,RDPipelineColorBlendStateAttachment,RDPipelineDepthStencilState,RDPipelineMultisampleState,RDPipelineRasterizationState,RDPipelineSpecializationConstant,RDSamplerState,RDShaderFile,RDShaderSPIRV,RDShaderSource,RDTextureFormat,RDTextureView,RDUniform,RDVertexAttribute,RandomNumberGenerator,RayCast2D,ReferenceRect,RegEx,RegExMatch,RemoteTransform2D,RenderData,RenderSceneBuffers,RenderSceneBuffersConfiguration,RenderSceneData,RenderingDevice,RenderingServer,ResourceFormatLoader,ResourceFormatSaver,ResourceImporter,ResourceLoader,ResourcePreloader,ResourceSaver,ResourceUID,RichTextEffect,RigidBody2D,SceneReplicationConfig,SceneState,ScriptBacktrace,ScriptExtension,ScriptLanguage,ScrollBar,SegmentShape2D,Semaphore,SeparationRayShape2D,Separator,ShaderGlobalsOverride,ShaderInclude,ShaderIncludeDB,Shape3D,ShapeCast2D,Shortcut,Skeleton2D,SkeletonModification2D,SkeletonModificationStack2D,SkeletonProfile,Skin,SkinReference,Sky,SpinBox,SplitContainer,StatusIndicator,StreamPeer,SubViewportContainer,SubtweenTweener,SurfaceTool,SyntaxHighlighter,SystemFont,TCPServer,TLSOptions,TabBar,TabContainer,TextEdit,TextLine,TextParagraph,TextServer,TextServerManager,Texture2DRD,Texture3D,TextureLayered,TextureProgressBar,ThemeContext,ThemeDB,Thread,TileData,TileMap,TileMapPattern,TileSetScenesCollectionSource,Time,TouchScreenButton,Translation,TranslationDomain,TranslationServer,Tree,TreeItem,TriangleMesh,UDPServer,UPNP,UPNPDevice,UndoRedo,UniformSetCacheRD,VSlider,VideoStream,VideoStreamPlayback,VideoStreamPlayer,VisibleOnScreenNotifier2D,VisualShader,VisualShaderNode,VoxelGIData,WeakRef,WebRTCPeerConnection,Window,WorkerThreadPool,World2D,World3D,WorldBoundaryShape2D,WorldEnvironment,X509Certificate,XMLParser,XRInterface,XRPose,XRServer,XRTracker,XRVRS,ZIPPacker,ZIPReader"

  SCONS_CACHE_MSVC_CONFIG: true
  PYTHONIOENCODING: utf8

jobs:
  build-templates:
    runs-on: windows-latest
    name: ${{ matrix.name }}
    timeout-minutes: 120
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Template (MSVC)
            cache-name: windows-template
            target: template_release
            scons-flags: windows_subsystem=windows
            bin: ./bin/godot.windows.template_release*.exe
            compiler: msvc

          - name: Template (MinGW)
            cache-name: windows-template-gcc
            target: template_release
            scons-flags: use_mingw=yes
            bin: ./bin/godot.windows.template_release*.exe
            compiler: gcc

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Restore Godot build cache
        uses: ./.github/actions/godot-cache-restore
        with:
          cache-name: ${{ matrix.cache-name }}
        continue-on-error: true

      - name: Setup Python and SCons
        uses: ./.github/actions/godot-deps

      - name: Download Direct3D 12 SDK components (minimal)
        id: d3d12-sdk
        shell: pwsh
        run: |
          try {
            python ./misc/scripts/install_d3d12_sdk_windows.py --minimal
            "D3D12_ENABLED=yes" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
          } catch {
            Write-Warning "Direct3D 12 SDK installation failed, building without Direct3D 12 support."
            "D3D12_ENABLED=no" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
            exit 0
          }
        continue-on-error: true

      - name: Download pre-built ANGLE static libraries
        uses: dsaltares/fetch-gh-release-asset@1.1.2
        with:
          repo: godotengine/godot-angle-static
          version: tags/chromium/6601.2
          file: godot-angle-static-x86_64-${{ matrix.compiler == 'gcc' && 'gcc' || 'msvc' }}-release.zip
          target: angle/angle.zip

      - name: Extract pre-built ANGLE static libraries
        shell: pwsh
        run: |
          Expand-Archive -Force angle/angle.zip ${{ github.workspace }}/

      - name: Download pre-built AccessKit
        uses: dsaltares/fetch-gh-release-asset@1.1.2
        with:
          repo: AccessKit/accesskit-c
          version: tags/0.17.0
          file: accesskit-c-0.17.0.zip
          target: accesskit-c-0.17.0/accesskit_c.zip

      - name: Extract pre-built AccessKit
        shell: pwsh
        run: |
          Expand-Archive -Force accesskit-c-0.17.0/accesskit_c.zip accesskit-c-0.17.0

      - name: Compilation (templates only)
        uses: ./.github/actions/godot-build
        with:
          # combine env SCONS_FLAGS and matrix overrides; pass detected d3d12 output
          scons-flags: ${{ env.SCONS_FLAGS }} ${{ matrix.scons-flags }} d3d12=${{ steps.d3d12-sdk.outputs.D3D12_ENABLED }}
          platform: windows
          target: ${{ matrix.target }}

      - name: Save Godot build cache
        uses: ./.github/actions/godot-cache-save
        with:
          cache-name: ${{ matrix.cache-name }}
        continue-on-error: true

      - name: List build outputs
        shell: pwsh
        run: |
          if (Test-Path -Path bin) {
            Get-ChildItem -Path bin -Recurse | Select-Object FullName,Length
          } else {
            Write-Error "bin directory not found"
            exit 1
          }

      - name: Prepare artifact (remove debug libs)
        if: matrix.compiler == 'msvc'
        shell: pwsh
        run: |
          Get-ChildItem -Path bin -Include *.exp,*.lib,*.pdb -Recurse -ErrorAction SilentlyContinue | Remove-Item -Force -ErrorAction SilentlyContinue || Write-Host "No debug artifacts to remove"

      - name: Upload artifact
        uses: ./.github/actions/upload-artifact
        with:
          name: ${{ matrix.cache-name }}
          # use a wildcard to be robust against slight filename differences
          path: ${{ matrix.bin }}
